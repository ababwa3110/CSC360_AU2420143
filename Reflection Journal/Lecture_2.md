Q1) How is computer graphics different from image processing?
Ans: Graphics is the generation of images, processing is the process that image for analysis, where you talk about acquisition, algorithms to understand the image, whereas graphics is generating the image

Q2) What are the geometric primitives of drawing?
Ans: Lines, curves, area, something that is atomic, fundamental and would be needed in order to do anything

Q3) Describe the graphical frameworks in Java?
Ans: AWT, swing, JavaFX, whether the module is part of the run time or is separate, and you need to attach

Q4) What is the design pattern for creating user interfaces? For eg clicking a button.
Ans: Event listener model, event sources, event firing, event models.

Q5) How is static graphics different from interactive graphics?
Ans: Interactive means repainting the analogy

Q6) How are curves connected with calculus?
Ans: Smooth curves, continuous, differentiable, used for drawing. Used to calculate areas with integration

Q7) Describe SSH and HTTPS protocols for accessing Git repositories

Q8) What are public and private keys, and how do you use them?
Ans: A digital image is nothing but a 2D matrix where at each cell you have a value which is a number, RGB code, or a single value. All you need in order to create an image is 1 variable that changes for every row and variable, a classroom analogy, each cell a student is sitting, score of 1 to 10, use that value to represent something

Q9) How do you create interactive inductance?
Ans: The mouse is all the time firing events. Every time it fires the events, it tells you where the cursor is. 

# Chapter 2: basic principles of 2D graphics

•	Raster v/s Vector graphics
•	Vector means you know how to draw and drawing logic, comp knows info is embedded and does it at runtime, during random. You give a dimension and it will draw on that canvas, no loss of resolution.
•	First java 2d program: Create a class, create a main program.
•	Geometric transformation: translation, scaling (zooming in and out).
•	Project Idea 1: Create a triangle and move your mouse to move the triangle around.
•	Project Idea 2: A triangle zooming in and zooming out.
•	Animation of movements based on transformation.
•	Interpolation - Change is not abrupt but rather continuous.
•	What is a pixel: The smallest unit of display.
•	Advantage of Windows over Linux: Fonts, coding experience is better, more readable, clearer, visual display, and compatibility with drivers.
